import { NoAccessCard } from "@shared/components/NoAccessCard";

const NoAccessPage = () => {
  return (
    <main className="mx-auto flex flex-1 max-w-2xl items-center justify-center px-4 py-8">
      <NoAccessCard className="w-full" />
    </main>
  );
};

export default NoAccessPage;
